# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ / Android Studio
*.iml
*.ipr
*.iws
.idea/

# VS Code (uncomment to track launch configs)
#.vscode/

# Flutter / Dart
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
.packages
coverage/
*.g.dart
*.freezed.dart

# Build outputs — any directory named "build" (not build.gradle files)
build/

# Native FFI CMake output (./wallet.sh build)
build/native_ffi/

# Android / Gradle / NDK
**/.gradle/
**/.cxx/
**/local.properties
**/GeneratedPluginRegistrant.java
android/app/debug/
android/app/profile/
android/app/release/
*.apk
*.aab
*.keystore
*.jks
key.properties

# iOS / macOS (Pods & Xcode; platform .gitignore files add more)
**/Pods/
**/DerivedData/
*.ipa
*.dSYM.zip
*.dSYM

# Windows
*.exe
*.dll
*.pdb

# Linux desktop bundle (local flutter build linux)
*.tar.gz

# Zentra source clone for native builds (use ../zentra or ZENTRA_ROOT)
third_party/zentra/

# Secrets / env
.env
.env.*
!.env.example

# Symbolication / obfuscation
app.*.symbols
app.*.map.json

# CI / release packaging at repo root
zentra-wallet-*.tar.gz
zentra-wallet-*.zip

# Committed on purpose for Linux CI & quick builds (do not ignore):
# packages/zentra_wallet_core/linux/libzentra_wallet_ffi.so
